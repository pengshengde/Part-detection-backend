stages:
  - build
  - deploy

build-springboot:
  stage: build
  script:
    - source java-env
  only:
    - main
  tags:
    - v0.2

deploy-springboot:
  stage: deploy
  script:
    - source java-env
    - zsh run.sh 
  only:
    - main
  tags:
    - v0.2