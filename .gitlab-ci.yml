stages:
  - build
  - deploy

build-springboot:
  stage: build
  script:
    - source java-env.d
  only:
    - main
  tags:
    - v0.2

deploy-springboot:
  stage: deploy
  script:
    - source java-env.d
    - zsh run.sh 
  only:
    - main
  tags:
    - v0.2